Backend require loading configuration from xml configuration file. 
Sample configuration file is located in ./configuration/config.xml. 
The path to the configuration file must be set as a system property "config". 
Use parameter -Dconfig=path_to_config.xml when running as standalone application.

When running from development environment the parameter -Dconfig must be passed to the JWM not to the application.  